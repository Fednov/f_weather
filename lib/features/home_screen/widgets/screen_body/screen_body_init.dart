import 'package:flutter/material.dart';

class ScreenBodyInit extends StatelessWidget {
  const ScreenBodyInit({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Выберите локацию'),
    );
  }
}
